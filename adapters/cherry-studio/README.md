# Cherry Studio integration (C-level)

Import `mcp.json.template` using Cherry Studio's documented stdio MCP settings.
The server exposes read-only, sanitized local evidence. Agent Doctor does not
read Cherry Studio's private database, transcripts, credentials, or headers.
Unsupported lifecycle, token, and cost signals remain unavailable.
