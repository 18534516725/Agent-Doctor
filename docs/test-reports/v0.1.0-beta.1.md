# Agent Doctor v0.1.0-beta.1 release evidence

Date: 2026-08-16 (Asia/Shanghai)

This report separates local evidence, GitHub CI evidence and published-release evidence. The `v0.1.0-beta.1` tag is public.

## Verified locally before first public push

- `go test ./... -count=1`: passed across all Go packages;
- `pnpm --dir dashboard test -- --run`: 7 files and 14 tests passed;
- `pnpm --dir dashboard build`: production bundle built;
- `./scripts/check-docs.sh`: public documentation contract passed;
- `./scripts/check-secrets.sh`: credential-shaped repository scan passed;
- `git diff --check`: passed;
- public `main` was pushed through SSH and the repository returned HTTP 200.

## Verified by GitHub CI

- workflow run `31929015492`: Linux, macOS and Windows verification jobs passed;
- the Windows runner created and migrated the SQLite database with a native drive-letter path;
- the release snapshot built every target archive and passed `verify-release.sh`;
- workflow run `31929264754`: the tagged release job completed successfully.

## Verified from the published release

- tag: `v0.1.0-beta.1` at commit `cde21c31f743fecb54042302cd1b41bee9cb6d3c`;
- six archives are present for macOS, Linux and Windows on amd64 and arm64;
- six matching SBOM files, `SHA256SUMS.txt`, both installers and
  `release-manifest.json` are public;
- all 16 published files were downloaded without repository credentials;
- `scripts/verify-release.sh` passed against the downloaded directory;
- every archive and SBOM matched `SHA256SUMS.txt`;
- sampled archive hashes:
  - macOS arm64: `2c4cd648409ea5994e946d9ba87a650bb118ea9ae919e0f389e3d8e62227cfee`;
  - Windows amd64: `2d86dd31344a62d1f76966b9099c39a0f4fe5747ff076c4e248eebe5bb520b7d`.

The remote installers now target this verified public beta. Real client-version
coverage remains a beta feedback item; publication does not imply every client
exposes the same evidence signals.
