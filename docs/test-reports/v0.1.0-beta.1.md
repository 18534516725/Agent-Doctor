# Agent Doctor v0.1.0-beta.1 release evidence

Date: 2026-08-16 (Asia/Shanghai)

This report separates local evidence, GitHub CI evidence and published-release evidence. A checked local item does not imply that a tagged release exists.

## Verified locally before first public push

- `go test ./... -count=1`: passed across all Go packages;
- `pnpm --dir dashboard test -- --run`: 7 files and 14 tests passed;
- `pnpm --dir dashboard build`: production bundle built;
- `./scripts/check-docs.sh`: public documentation contract passed;
- `./scripts/check-secrets.sh`: credential-shaped repository scan passed;
- `git diff --check`: passed;
- public `main` was pushed through SSH and the repository returned HTTP 200.

## Still required for the tag

- Linux, macOS and Windows CI jobs complete successfully;
- release snapshot verification succeeds;
- the tag workflow publishes platform archives, SBOMs and `SHA256SUMS.txt`;
- `release-manifest.json` is uploaded;
- one published archive is downloaded anonymously and independently verified;
- installer and NexoToken product links return successfully.

Until all tag requirements pass, source installation is the honest default and the remote installer is documented as conditional.
