import type { PageProps } from './types';
export function TrendsPage({ locale, snapshot, analysis }: PageProps) { const max = Math.max(1, ...snapshot.trends.map((point) => point.events)); return <div className="trend-board"><div className="trend-summary"><strong>{Math.round(analysis.averageLatencyMs)}<small> ms</small></strong><span>{locale === 'zh' ? '平均响应时间' : 'Average latency'}</span></div><div className="trend-bars">{snapshot.trends.length === 0 ? <p>{locale === 'zh' ? '数据积累后，这里会显示真实趋势。' : 'Real trends will appear after data accumulates.'}</p> : snapshot.trends.map((point) => <article key={point.date}><time>{point.date}</time><div><i style={{ width: `${Math.max(3, point.events / max * 100)}%` }} /></div><strong>{point.events}</strong></article>)}</div></div>; }
