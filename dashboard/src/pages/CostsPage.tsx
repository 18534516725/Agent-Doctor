import type { PageProps } from './types';
const fmt = new Intl.NumberFormat('zh-CN');
export function CostsPage({ locale, analysis }: PageProps) { const total = analysis.inputTokens + analysis.outputTokens; return <div className="ledger"><article className="ledger-hero"><span>USAGE / COST</span><strong>{fmt.format(total)}</strong><p>{locale === 'zh' ? '累计 Token' : 'Total tokens'}</p></article><div className="ledger-grid"><Ledger value={analysis.inputTokens} label={locale === 'zh' ? '输入 Token' : 'Input tokens'} /><Ledger value={analysis.outputTokens} label={locale === 'zh' ? '输出 Token' : 'Output tokens'} /><Ledger value={analysis.cachedTokens} label={locale === 'zh' ? '缓存命中' : 'Cached tokens'} /><Ledger value={analysis.reasoningTokens} label={locale === 'zh' ? '推理 Token' : 'Reasoning tokens'} /></div><div className="precision-note"><strong>{locale === 'zh' ? '费用证据' : 'Cost evidence'}</strong><p>{analysis.unknownCostCount ? (locale === 'zh' ? `${analysis.unknownCostCount} 条请求没有可核验的价格目录，因此不会伪造为 ¥0。` : `${analysis.unknownCostCount} requests lack a verified price catalog and are not displayed as zero.`) : `$${(analysis.exactCostMicros / 1_000_000).toFixed(6)}`}</p></div></div>; }
function Ledger({ value, label }: { value: number; label: string }) { return <article><strong>{fmt.format(value)}</strong><span>{label}</span></article>; }
