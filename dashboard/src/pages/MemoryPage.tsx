import type { PageProps } from './types';
export function MemoryPage({ locale, snapshot }: PageProps) { return <div className="state-board">{[['active', snapshot.memories.active], ['candidate', snapshot.memories.candidate], ['disabled', snapshot.memories.disabled]].map(([kind, value]) => <article key={kind}><span>{kind}</span><strong>{value}</strong><p>{locale === 'zh' ? ({ active: '已确认并可复用', candidate: '等待你确认', disabled: '已停用但未删除' }[kind as string]) : ({ active: 'Confirmed and reusable', candidate: 'Waiting for review', disabled: 'Disabled, not deleted' }[kind as string])}</p></article>)}</div>; }
